{% set name = "datatoolbox" %}
{% set version = "0.7.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/datatoolbox-{{ version }}.tar.gz
  sha256: 33d883960461fc4ed5a291b02b8e8b1b3a65af2632c59e8aed25a2cf968cadd4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools-scm
  run:
    - appdirs
    - deprecated
    - fuzzywuzzy
    - gitpython
    - networkx >=2.4.0
    - matplotlib-base
    - numpy
    - openpyxl
    - openscm-units >0.5
    - pandas >=1.1.0
    - pandas-indexing
    - pint >=0.11
    - pint-xarray
    - pyam >=1.4.0
    - pycountry
    - pyarrow
    - python >=3.6
    - python-docx
    - tabulate
    - treelib
    - tqdm
    - xarray
    - xlsxwriter

test:
  imports:
    - datatoolbox
  commands:
    - pip check
  requires:
    - pip
    - git

about:
  home: https://gitlab.com/climateanalytics/datatoolbox
  summary: The Python Data Toolbox
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - coroa
    - geiges
